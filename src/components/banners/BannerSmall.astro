---
import { getImage } from "astro:assets";
import myBackground from "../../assets/imgs/bannerProducts.jpg";

const optimizedBackground = await getImage({
  src: myBackground,
  format: "jpg",
});
---

<section
  class="w-11/12 md:container relative mt-0 mb-4 lg:mb-8
  rounded-b-3xl overflow-hidden
  h-72 lg:h-96
  bg-[length:auto_100%] md:bg-cover"
  style={`background-image: url(${optimizedBackground.src});`}
>
  <p
    class="banner__title
    absolute bg-tertiary-500 text-gray-50
    font-bold font-title text-2xl lg:text-3xl
    py-4 pl-4 lg:py-12 lg:pl-12
    rounded-tr-full lg:rounded-r-3xl
    bottom-0 left-0
    w-full max-w-80 md:max-w-96
    tracking-[1.70px] leading-[1.5rem]"
  >
    El toque de estilo que <br /> tus espacios merecen
  </p>
</section>

<style></style>
